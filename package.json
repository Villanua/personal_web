{
  "name": "villanua-web-server",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=22.0.0" 
  },
  "scripts": {
    "install-all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}